export namespace Types {
    export interface Video {
        thumbUrl?: string;
        profileUrl?: string;
        title?: string;
        username?: string;
        viewcount?: string;
        releaseDate?: string;
    }
}
