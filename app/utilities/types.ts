export namespace Types {
    export interface Video {
        thumbUrl?: String,
        profileUrl?: String
    }

    export interface Details {
        title?: String,
        username?: String,
        viewcount?: String,
        releaseDate?: String
    }
}